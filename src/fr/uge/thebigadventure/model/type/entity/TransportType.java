package fr.uge.thebigadventure.model.type.entity;

/**
 * TransportType is an enum that represents the different types of transport.
 * Currently, these transport types are not used in the game.
 */
public enum TransportType implements EntityType {
  PLANE, ROCKET, UFO,
  CAR,
  CART, TRAIN,
  BOAT
}
