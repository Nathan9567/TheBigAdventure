package fr.uge.thebigadventure.model.type.entity;

/**
 * EffectType is an enum representing the different types of effects.
 * It can be uses to create an effect at a specific position.
 */
public enum EffectType implements EntityType {
  BUBBLE, DUST
}
