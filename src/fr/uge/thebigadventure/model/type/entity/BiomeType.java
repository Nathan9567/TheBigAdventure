package fr.uge.thebigadventure.model.type.entity;

/**
 * BiomeType is an enum that represents the different types of biome.
 */
public enum BiomeType implements EntityType {
  ICE, LAVA, WATER
}
